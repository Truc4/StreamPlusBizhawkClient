{
  "name": "streamplusbizhawk-broadcaster-client",
  "version": "1.0.0",
  "description": "Connects to the StreamPlusBizhawk server and gets data from the exextension",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Truc_e",
  "license": "ISC",
  "bin": "app.js",
  "dependencies": {
    "socket.io-client": "^2.2.0"
  }
}
